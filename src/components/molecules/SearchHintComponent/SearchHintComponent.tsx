import { HintWrapper } from "./SearchHintComponent.styles"

interface Props {
     searchInputText: string;
  }

const SearchHintComponent = ({ searchInputText }: Props) => {
  

  
  return <HintWrapper>hint1</HintWrapper>
}

export default SearchHintComponent;